import React from 'react';
import Header from '../Header/Header';

const Policy = () => {
    return (
        <div>
            <Header></Header>
            <h1>Our policies</h1>
            <p>It is important to us to have clear ideas and frameworks as a basis in our efforts to be the best employer we can be. For that reason, we have a set of internal regulations which together with the external rules make up the framework for our operations.</p>
        </div>
    );
};

export default Policy;